# Dashboard App (Vite + React)

## Run

```bash
cd ops_dashboard/dashboard_app
npm install
npm run dev
```

The app reads `../state.json` via a Vite middleware endpoint at `/state.json`.
The UI refreshes every 5 seconds.
